{
  "comparison": "Previously recorded chart-restore-full.json; includes its backend candle adapter tests, so total test counts differ.",
  "newFailureNames": [],
  "existingFailureNames": [
    "30D keeps lifetime statistics separate from the selected chart period",
    "7D keeps lifetime statistics separate from the selected chart period",
    "90D keeps lifetime statistics separate from the selected chart period",
    "ALL keeps lifetime statistics separate from the selected chart period",
    "Copy preserves its approved source except exact labels and click-only deposit requirement UX",
    "CopyButton remains byte-equivalent to approved V8",
    "Futures UI-only reconciliation components/FuturesAccountSummary.tsx preserves non-visual semantics",
    "Futures UI-only reconciliation components/FuturesOrderForm.tsx preserves non-visual semantics",
    "Futures UI-only reconciliation components/FuturesPairList.tsx preserves non-visual semantics",
    "Futures UI-only reconciliation components/OrderBookPanel.tsx preserves non-visual semantics",
    "Futures UI-only reconciliation pages/FuturesPage.tsx preserves non-visual semantics",
    "MetricsPanel remains byte-equivalent to approved V8",
    "MiniPerformanceChart remains byte-equivalent to approved V8",
    "Nazara marketplace presentation only 30D consumes canonical ROI, win rate, drawdown, followers and AUM without mutating input",
    "Nazara marketplace presentation only 7D consumes canonical ROI, win rate, drawdown, followers and AUM without mutating input",
    "Nazara marketplace presentation only 90D consumes canonical ROI, win rate, drawdown, followers and AUM without mutating input",
    "Nazara marketplace presentation only ALL consumes canonical ROI, win rate, drawdown, followers and AUM without mutating input",
    "Nazara marketplace presentation only Nazar VIP and the real verified badge share the name row, without a second status line",
    "Nazara marketplace presentation only VIP remains independent and identity verification never inherits the static catalogue flag",
    "Nazara marketplace presentation only ordinary card content is preserved; absent Nazara metrics do not become fake constants",
    "Nazara marketplace presentation only ordinary marketplace cards use the same real inline SVG, without hardcoded flagship names",
    "Nazara marketplace presentation only the real card renders identityVerified=false beside the name, never over the avatar",
    "Nazara marketplace presentation only the real card renders identityVerified=true beside the name, never over the avatar",
    "Nazara marketplace presentation only the real card renders identityVerified=undefined beside the name, never over the avatar",
    "ProfilePerformanceChart remains byte-equivalent to approved V8",
    "Spot orders truthful dense presentation keeps the complete shared Futures stylesheet intact and scopes every new override to Spot",
    "Spot tiny-price rows keep unchanged full numeric labels and narrowly scoped non-overlapping cells",
    "actual histogram markup uses the same uncapped signed geometry and canonical tooltips",
    "actual onError removes broken image, retains geometry and retries changed owner URL",
    "actual profile shows identityVerified=false only inline with its heading",
    "actual profile shows identityVerified=true only inline with its heading",
    "actual profile shows identityVerified=undefined only inline with its heading",
    "actual stateful BTC → MOG → BTC book initializes grouping before paint and retains manual same-pair steps",
    "approved yellow chart, histogram, statistics, trades and hero stay source-identical before additive Ksenia wiring",
    "copy-card polish retains disabled/Following states and uses scoped, readable desktop/mobile styles",
    "customer-facing components render no provider branding has no provider name in any executable line",
    "fallback translations remain available in all seven locales",
    "loaded script without a widget times out and can retry, no poisoned global promise",
    "locale switch remounts cleanly: en",
    "locale switch remounts cleanly: es",
    "locale switch remounts cleanly: ja",
    "locale switch remounts cleanly: ru",
    "locale switch remounts cleanly: zh",
    "market palette preserves official symbol and native controls: cfd",
    "market palette preserves official symbol and native controls: futures",
    "market palette preserves official symbol and native controls: spot",
    "native TradingView indicators toolbar is enabled and volume remains controllable",
    "no route stylesheet may move the shared authenticated header and none of them shifts a box without excluding the header",
    "no route stylesheet may move the shared authenticated header finds the page-root resets this repo actually ships",
    "nobody fetches /futures/config except the store the endpoint has exactly one client method, and one caller",
    "normal API paths replace review-only sources without altering token or request behavior",
    "normalization only reverses exact approved UX edits, never hides copy-action or financial drift",
    "one chart across ten rapid switches, detached late work cannot stack: cfd",
    "one chart across ten rapid switches, detached late work cannot stack: futures",
    "one chart across ten rapid switches, detached late work cannot stack: spot",
    "preserves existing financial/data/format/modal source frontend/src/lib/api.ts exactly",
    "preserves existing financial/data/format/modal source frontend/src/pages/wallet-v3/DepositModal.tsx exactly",
    "script failure shows honest fallback, preserves CFD disclaimer and retry recovers",
    "selective Card promotion preserves the exact approved page, composition, copy and state machine",
    "spot and futures pair lists are unchanged leaves the pair-list modules byte-identical",
    "switching Spot to Futures replaces the owned chart with the new palette and contract",
    "the homepage does not borrow another route's stylesheet 7. route splitting is intact: the homepage is eager, the rest are not",
    "the homepage does not borrow another route's stylesheet the two borrowed stylesheets are still reachable ONLY from their lazy pages",
    "the shared authenticated header does not depend on a lazy stylesheet and no route stylesheet declares a rule whose subject is a header class",
    "the shared authenticated header does not depend on a lazy stylesheet and the shared burger is index.css's alone, at every width",
    "the utilities layers in this repo are the three we think they are exactly three stylesheets carry @tailwind utilities, each named",
    "timeframe label is driven by the exact interval sent to the chart; controls cannot drift",
    "translation integrity preserves the exact source bytes of every dictionary body",
    "unknown CFD mapping and malformed symbols never substitute another instrument",
    "unmount pending script cancels callbacks and removes all owned nodes",
    "verified CFD mapping and compact visible attribution are preserved"
  ]
}
