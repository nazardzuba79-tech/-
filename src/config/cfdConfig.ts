// CFD's existing account-age policy is independent of Futures notional tiers.
// Relocated unchanged so removing the Futures age cap does not change CFD.
export const NEW_ACCOUNT_MAX_LEVERAGE = 10;
export const NEW_ACCOUNT_PERIOD_DAYS = 30;
