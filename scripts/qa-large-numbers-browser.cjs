/** Large values through the ORIGINAL /futures form, summary, tables and P&L card.
 * Shares the isolated runner instead of mounting the removed legacy ticket/panel.
 * Synthetic layout responses are scoped to its disposable local fixture server.
 */
process.env.NATIVE_QA_LARGE_ONLY = '1';
require('./qa-native-demo-browser.cjs');
