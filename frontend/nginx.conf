server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: any path that isn't a real file falls back to index.html
    # so React Router (client-side routing) handles it instead of a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
